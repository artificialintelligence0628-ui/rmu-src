# Optional in dev (Vite proxies /api to localhost:5000 already — see vite.config.js).
# Leave unset in production; the client calls same-origin /api on the single Render service.
# VITE_API_URL=http://localhost:5000/api
VITE_PAYSTACK_PUBLIC_KEY=pk_test_xxx
